/*
import { Action, ActionCreator } from 'redux';
import { ResourceDomain } from '../../models';


export const SET_APP_RESOURCE_DOMAINS = 'Set application resource domains';
export interface SetAppResourceDomains extends Action {
    appResourceDomains: ResourceDomain[]
}

export const setAppResourceDomains: ActionCreator<SetAppResourceDomains> =
    (r) => ({

        type: SET_APP_RESOURCE_DOMAINS,
        appResourceDomains: r
    });
*/